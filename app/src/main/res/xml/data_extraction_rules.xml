<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device through backup or device transfer.

  The database holds conversation history and file paths, and the keystore
  holds API keys. Neither belongs in a cloud backup the user did not ask for,
  so both cloud backup and device-to-device transfer are excluded wholesale.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
